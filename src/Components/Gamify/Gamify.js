import React, { useState } from 'react';
import { getPrediction } from '../DriverBehave/DriverBehave';

const Gamify = () => {
  const value = getPrediction();

  // Use the state variable value in this component

  return (
    <>
        
    </>
  );
};

export default Gamify;
